class Planet
  # keep it low and > 0 for dev
  BODIES_STATISTICS = (1..3)

  # TODO don't copy symbols like this
  TYPES_STATISTICS = %i(asteroid_belt planet planet planet)

  # keep it low for dev
  MOONS_STATISTICS = (0..2)
end
